// import {CPU} from "../../../src/cpu/cpu";
// import {instruction as i} from "../../../src/cpu/instruction";

describe("MA CPU tests", () => {

	// it("should load word \"LW\"", () => {
	// 	const cpu = new CPU();
	// 	const instruction = i(0x8d090000);
	// 	// cpu.ma.LW(instruction);
	//
	// });

	it("should run test placeholder", () => {
		expect(1).toBe(1);
	});

});
